TridiagonalMatmul:
  target_api: '''mx.expand_dims({1}, -1) * {3} + mx.expand_dims(mx.concatenate([mx.zeros_like({0}[...,
    :1]), {0}[..., 1:]], axis=-1), -1) * mx.concatenate([mx.zeros_like({3}[..., :1,
    :]), {3}[..., :-1, :]], axis=-2) + mx.expand_dims(mx.concatenate([{2}[..., :-1],
    mx.zeros_like({2}[..., -1:])], axis=-1), -1) * mx.concatenate([{3}[..., 1:, :],
    mx.zeros_like({3}[..., -1:, :])], axis=-2)'''
