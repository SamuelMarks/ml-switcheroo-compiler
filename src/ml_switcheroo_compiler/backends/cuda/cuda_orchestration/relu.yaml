relu:
  body: "__global__ void relu(const float* A, float* C, int N) {\n    int id = blockIdx.x\
    \ * blockDim.x + threadIdx.x;\n    if (id < N) {\n        C[id] = fmaxf(0.0f,\
    \ A[id]);\n    }\n}\n"
  workgroup_size:
  - 256
  - 1
  - 1
