mul:
  body: "__global__ void mul(const float* A, const float* B, float* C, int N) {\n\
    \    int id = blockIdx.x * blockDim.x + threadIdx.x;\n    if (id < N) {\n    \
    \    C[id] = A[id] * B[id];\n    }\n}\n"
  workgroup_size:
  - 256
  - 1
  - 1
