operation: Full
backend: pytorch
target_api: ''
ast_template: torch.full({shape}, {fill_value})' + (", dtype=getattr(torch, '" + str(kwargs.get('dtype'))
  + "', torch.float32)" if 'dtype' in kwargs else '')
kwarg_map: {}
dtype_overrides: {}
