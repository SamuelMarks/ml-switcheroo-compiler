operation: Ones
backend: pytorch
target_api: ''
ast_template: torch.ones({shape})' + (", dtype=getattr(torch, '" + str(kwargs.get('dtype'))
  + "', torch.float32)" if 'dtype' in kwargs else '')
kwarg_map: {}
dtype_overrides: {}
