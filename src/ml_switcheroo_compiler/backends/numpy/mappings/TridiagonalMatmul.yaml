TridiagonalMatmul:
  ast_template: np.expand_dims({1}, -1) * {3} + np.expand_dims(np.concatenate([np.zeros_like({0}[...,
    :1]), {0}[..., 1:]], axis=-1), -1) * np.concatenate([np.zeros_like({3}[..., :1,
    :]), {3}[..., :-1, :]], axis=-2) + np.expand_dims(np.concatenate([{2}[..., :-1],
    np.zeros_like({2}[..., -1:])], axis=-1), -1) * np.concatenate([{3}[..., 1:, :],
    np.zeros_like({3}[..., -1:, :])], axis=-2)
  target_api: ''
