Gelu:
  body: '

    let idx = global_id.x;

    if (idx >= {nelem}u) { return; }

    let x = buf_in_f32[idx];

    buf_out_f32[idx] = 0.5 * x * (1.0 + tanh(0.7978845608 * (x + 0.044715 * x * x
    * x)));

    '
  workgroup_size:
  - 64
  - 1
  - 1
