Gather:
  body: '

    let idx = global_id.x;

    if (idx >= {out_size}u) { return; }

    let in_idx = buf_indices_i32[idx];

    buf_out_f32[idx] = buf_in_f32[in_idx];

    '
  workgroup_size:
  - 64
  - 1
  - 1
