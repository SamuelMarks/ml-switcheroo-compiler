Min:
  body: "\nlet idx = global_id.x;\nif (idx >= {out_size}u) { return; }\nvar min_val\
    \ = 3.402823466e+38f;\nfor (var i = 0u; i < {reduce_size}u; i = i + 1u) {\n  \
    \  let val = buf_in_f32[idx * {reduce_size}u + i];\n    if (val < min_val) { min_val\
    \ = val; }\n}\nbuf_out_f32[idx] = min_val;\n"
  workgroup_size:
  - 64
  - 1
  - 1
