ReduceMean:
  jvp: ReduceMean($tangent[0], axes=$axes, keepdims=$keepdims)
  vjp:
  - '- Div(BroadcastTo($cotangent, ShapeOf($input[0])), Cast(SizeOf($input[0], axes=$axes),
    DtypeOf($input[0])))'
