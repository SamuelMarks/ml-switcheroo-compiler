CrossEntropyLoss:
  jvp: ReduceMean(Mul(Sub(Softmax($input[0]), $input[1]), $tangent[0]))
  vjp:
  - Mul($cotangent, Div(Sub(Softmax($input[0]), $input[1]), Cast(Numel($input[0]),
    Constant(1.0))))
  - Mul($cotangent, Neg(LogSoftmax($input[0])))
