LinearGeluFusion:
  pattern:
    capture: gelu
    inputs:
    - capture: linear
      inputs:
      - capture: x
      - capture: w
      - capture: b
      op_type: Linear
    op_type: GELU
  replacement:
    capture_to_replace: gelu
    inputs:
    - x
    - w
    - b
    op_type: LinearGelu
