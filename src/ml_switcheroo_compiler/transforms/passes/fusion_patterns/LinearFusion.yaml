LinearFusion:
  pattern:
    capture: add
    inputs:
    - capture: matmul
      inputs:
      - capture: x
      - capture: w
      op_type: MatMul
    - capture: b
    op_type: Add
  replacement:
    capture_to_replace: add
    inputs:
    - x
    - w
    - b
    op_type: Linear
