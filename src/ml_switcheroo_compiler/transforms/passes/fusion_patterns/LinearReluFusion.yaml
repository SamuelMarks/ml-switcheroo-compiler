LinearReluFusion:
  pattern:
    capture: relu
    inputs:
    - capture: linear
      inputs:
      - capture: x
      - capture: w
      - capture: b
      op_type: Linear
    op_type: Relu
  replacement:
    capture_to_replace: relu
    inputs:
    - x
    - w
    - b
    op_type: LinearRelu
