ElementwiseFusion:
  pattern:
    capture: relu
    inputs:
    - capture: add
      inputs:
      - capture: in1
      - capture: in2
      op_type: Add
    op_type: Relu
  replacement:
    capture_to_replace: relu
    inputs:
    - in1
    - in2
    op_type: AddRelu
