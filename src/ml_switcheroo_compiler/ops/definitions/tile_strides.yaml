operation: tile_strides
description: Tiles the trailing strides in `strides` according to `tiling`.
std_args:
- strides
- tiling
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
