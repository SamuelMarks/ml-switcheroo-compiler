operation: prune
description: Compute and returns a pruned version of input tensor ``t``.
std_args:
  - self
  - t
  - default_mask
  - importance_scores
type: function
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
