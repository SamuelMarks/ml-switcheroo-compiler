operation: NotOfType
description: States that `expr` is not an instance of `type`.
std_args:
- self
- expr
- type
type: class
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
