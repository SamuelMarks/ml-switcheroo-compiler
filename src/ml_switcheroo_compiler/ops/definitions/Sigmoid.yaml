operation: Sigmoid
description: The Sigmoid operation.
std_args: []
type: function
variants:
  edge_onnx:
    generator: Sigmoid
  edge_stablehlo:
    generator: stablehlo.logistic
  keras:
    eager: keras_ops.sigmoid
  numpy:
    generator: scipy.special.expit({0})
  tensorflow:
    eager: tf.sigmoid
  cupy: {}
  dask: {}
  pytorch: {}
  jax: {}
  mlx: {}
  edge_wgsl:
    template: unary
    expr: 1.0 / (1.0 + exp(-buf_in0_f32[in0_offset]))
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
autodiff:
  vjp:
  - Multiply($cotangent, Multiply(Sigmoid($input[0]), Subtract(Constant(1.0), Sigmoid($input[0]))))
  jvp: Multiply($tangent[0], Multiply(Sigmoid($input[0]), Subtract(Constant(1.0),
    Sigmoid($input[0]))))
