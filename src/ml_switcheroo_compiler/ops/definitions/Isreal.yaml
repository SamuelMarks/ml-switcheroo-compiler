operation: isreal
description: The Isreal operation.
std_args: []
variants:
  cupy:
    eager: cp.isreal
  dask:
    eager: da.isreal
  numpy:
    eager: numpy.isreal
  tensorflow:
    eager: tf.isreal
  keras:
    eager: keras_ops.isreal
  pytorch:
    eager: torch.isreal
  jax:
    eager: jax.numpy.isreal
  mlx:
    eager: mlx.core.isreal
  edge_onnx:
    generator: isreal
  edge_stablehlo:

    opcode: stablehlo.isreal
  edge_wgsl:
    template: binary
    expr: f32(buf_in0_f32[in0_offset] != 0.0 && buf_in1_f32[in1_offset] != 0.0)
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
