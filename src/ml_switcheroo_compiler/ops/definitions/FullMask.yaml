operation: FullMask
description: Lazy full mask, allows all tokens to attend to all other tokens.
std_args:
  - self
  - _shape
type: class
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
