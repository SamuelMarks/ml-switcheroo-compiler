operation: MultiHeadMask
description: Lazy multihead mask, combines multiple lazy masks one per head.
std_args:
  - self
  - masks
type: class
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
