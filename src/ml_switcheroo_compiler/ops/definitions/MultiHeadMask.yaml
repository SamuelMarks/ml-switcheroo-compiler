operation: MultiHeadMask
description: Lazy multihead mask, combines multiple lazy masks one per head.
std_args:
- self
- masks
type: class
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
