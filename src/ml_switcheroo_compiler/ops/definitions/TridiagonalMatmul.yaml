operation: TridiagonalMatmul
description: The TridiagonalMatmul operation.
variants:
  tensorflow:
    eager: tf.linalg.tridiagonal_matmul
  numpy:
    eager: numpy.tridiagonalmatmul
  cupy: {}
  dask: {}
  keras:
    eager: keras_ops.tridiagonalmatmul
  pytorch:
    eager: torch.tridiagonalmatmul
  jax:
    eager: jax.numpy.tridiagonalmatmul
  mlx:
    eager: mlx.core.tridiagonalmatmul
  edge_onnx:
    generator: TridiagonalMatmul
  edge_stablehlo:

    opcode: stablehlo.tridiagonal_matmul
  edge_wgsl:
    template: binary
    expr: buf_in0_f32[in0_offset] * buf_in1_f32[in1_offset]
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
