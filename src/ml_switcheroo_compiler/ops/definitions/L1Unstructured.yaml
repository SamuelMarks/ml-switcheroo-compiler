operation: L1Unstructured
description: Prune (currently unpruned) units in a tensor by zeroing out the ones
  with the lowest L1-norm.
std_args:
  - self
  - amount
type: class
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
