operation: isnan
description: The Isnan operation.
std_args:
  - is_variadic: false
    kind: positional_or_keyword
    name: x
    type: array
variants:
  cupy:
    eager: cp.isnan
  dask:
    eager: da.isnan
  keras:
    eager: keras_ops.isnan
  numpy:
    eager: numpy.isnan
  tensorflow:
    eager: tf.isnan
  pytorch:
    eager: torch.isnan
  jax:
    eager: jax.numpy.isnan
  mlx:
    eager: mlx.core.isnan
  edge_onnx:
    generator: isnan
  edge_stablehlo:

    opcode: stablehlo.isnan
  edge_wgsl:
    template: unary
    expr: isNaN(buf_in0_f32[in0_offset])
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
