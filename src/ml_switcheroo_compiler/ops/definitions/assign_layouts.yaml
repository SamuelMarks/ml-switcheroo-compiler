operation: assign_layouts
description: Assigns the layouts in `solution` to the MLIR ops they belong to.
std_args:
- solution
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
