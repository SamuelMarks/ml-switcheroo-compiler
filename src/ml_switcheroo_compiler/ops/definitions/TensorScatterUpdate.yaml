operation: TensorScatterUpdate
description: The TensorScatterUpdate operation.
variants:
  pytorch:
    generator: '{0}.clone().index_put_(tuple({1}.unbind(-1)), {2})'
  numpy:
    eager: numpy.tensorscatterupdate
  cupy: {}
  dask: {}
  tensorflow:
    eager: tf.tensorscatterupdate
  keras:
    eager: keras_ops.tensorscatterupdate
  jax:
    eager: jax.numpy.tensorscatterupdate
  mlx:
    eager: mlx.core.tensorscatterupdate
  edge_onnx:
    generator: TensorScatterUpdate
  edge_stablehlo:

    opcode: stablehlo.tensor_scatter_update
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
