operation: LuMatrixInverse
description: The LuMatrixInverse operation.
variants:
  numpy:
    generator: np.linalg.inv(np.take_along_axis(np.matmul(np.tril({0}, -1) + np.eye({0}.shape[-1],
      dtype={0}.dtype), np.triu({0})), np.broadcast_to(np.expand_dims(np.argsort({1},
      axis=-1), -1), {0}.shape), axis=-2))
  tensorflow:
    eager: tf.linalg.lu_matrix_inverse
  cupy: {}
  dask: {}
  keras:
    eager: keras_ops.lumatrixinverse
  pytorch:
    eager: torch.lumatrixinverse
  jax:
    eager: jax.numpy.lumatrixinverse
  mlx:
    eager: mlx.core.lumatrixinverse
  edge_onnx: {}
  edge_stablehlo: {}
  edge_wgsl:
    template: unary
    expr: buf_in0_f32[in0_offset]
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
