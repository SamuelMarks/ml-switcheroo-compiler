operation: MatrixNorm
description: The MatrixNorm operation.
variants:
  numpy:
    generator: np.linalg.norm({0}, keepdims={keepdims})
  cupy: {}
  dask: {}
  tensorflow: {}
  keras: {}
  pytorch: {}
  jax: {}
  mlx: {}
  edge_onnx:

    generator: MatrixNorm
  edge_stablehlo:

    opcode: stablehlo.matrix_norm
  edge_wgsl:
    template: conv_pool_1d_flat
    expr: buf_in0_f32[idx]
  edge_wasm_simd:
    template: conv_pool_1d_flat
    math_op: '* 1.0f'
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
