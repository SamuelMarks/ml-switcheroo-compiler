operation: is_known_divisible
description: Returns True if the value is statically known to be divisible by the
  divisor.
std_args:
  - value
  - divisor
  - max_depth
type: function
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
