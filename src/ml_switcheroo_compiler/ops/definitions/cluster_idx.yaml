operation: cluster_idx
description: Returns the linear index of a block within a subset of the cluster spanned
  by the given dimensions.
std_args:
  - dim
  - dim_idx
type: function
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
