operation: sync_global_devices
description: Creates a barrier across all hosts/devices.
std_args:
- name
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
