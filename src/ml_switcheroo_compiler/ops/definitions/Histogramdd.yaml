operation: histogramdd
description: The Histogramdd operation.
std_args: []
variants:
  numpy:
    generator: np.histogramdd
  cupy: {}
  dask: {}
  tensorflow:
    eager: tf.histogramdd
  keras:
    eager: keras_ops.histogramdd
  pytorch:
    eager: torch.histogramdd
  jax:
    eager: jax.numpy.histogramdd
  mlx:
    eager: mlx.core.histogramdd
  edge_onnx:
    generator: histogramdd
  edge_stablehlo:

    opcode: stablehlo.histogramdd
  edge_wgsl:
    template: binary
    expr: f32(buf_in0_f32[in0_offset] != 0.0 && buf_in1_f32[in1_offset] != 0.0)
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
