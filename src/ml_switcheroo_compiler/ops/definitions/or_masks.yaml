operation: or_masks
description: Returns a mask_mod that's the union of provided mask_mods
std_args:
- mask_mods
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
