operation: prime_decomposition
description: Returns the prime decomposition of the given number `n` as a list of
  ints.
std_args:
  - n
type: function
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
