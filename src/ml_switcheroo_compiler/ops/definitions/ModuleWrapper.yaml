operation: ModuleWrapper
description: A subclass of ``torch.nn.Module`` that wraps a C++ frontend module and
  delegates all access.
std_args:
- self
- cpp_module
type: class
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
