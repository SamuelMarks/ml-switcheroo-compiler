operation: tile_offset
description: Tiles the trailing offsets in `offsets` according to `tiling`.
std_args:
- offsets
- tiling
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
