operation: FORCE_USE_FLEX_ATTENTION
description: If True, forces the use of the flex attention kernel instead of potentially
  using
std_args: []
type: attribute
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
