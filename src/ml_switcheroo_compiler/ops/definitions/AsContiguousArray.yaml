operation: AsContiguousArray
description: Return a contiguous array (ndim >= 1) in memory (C order).
std_args:
- is_variadic: false
  kind: positional_or_keyword
  name: a
  type: Tensor
- is_variadic: false
  kind: positional_or_keyword
  name: dtype
  type: str
variants:
  numpy:
    eager: numpy.ascontiguousarray
  cupy: {}
  dask: {}
  tensorflow:
    eager: tf.ascontiguousarray
  keras:
    eager: keras_ops.ascontiguousarray
  pytorch:
    eager: torch.ascontiguousarray
  jax:
    eager: jax.numpy.ascontiguousarray
  mlx:
    eager: mlx.core.ascontiguousarray
  edge_onnx: {}
  edge_stablehlo: {}
  edge_wgsl:
    template: unary
    expr: buf_in0_f32[in0_offset]
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
