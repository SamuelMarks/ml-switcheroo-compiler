operation: get_tpu_version
description: Returns the numeric version of the TPU, or -1 if not on TPU.
std_args: []
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
