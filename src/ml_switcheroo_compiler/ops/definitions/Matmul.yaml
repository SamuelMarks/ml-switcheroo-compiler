operation: MatMul
description: The MatMul operation.
std_args:
- is_variadic: false
  kind: positional_or_keyword
  name: x1
  type: array
- is_variadic: false
  kind: positional_or_keyword
  name: x2
  type: array
type: attribute
variants:
  edge_onnx:
    generator: MatMul
  edge_stablehlo:
    generator: stablehlo.dot_general
  edge_wgsl:
    template: matmul
  edge_wasm_simd:
    template: matmul
  llvm_cpp:
    template: matmul
autodiff:
  vjp:
  - MatMul($cotangent, Transpose($input[1]))
  - MatMul(Transpose($input[0]), $cotangent)
  jvp: Add(MatMul($tangent[0], $input[1]), MatMul($input[0], $tangent[1]))
