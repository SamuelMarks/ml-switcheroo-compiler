operation: If
description: The If operation.
variants:
  edge_onnx:
    generator: If
  edge_stablehlo:
    generator: stablehlo.if
  numpy:
    eager: numpy.if
  cupy: {}
  dask: {}
  tensorflow:
    eager: tf.if
  keras:
    eager: keras_ops.if
  pytorch:
    eager: torch.if
  jax:
    eager: jax.numpy.if
  mlx:
    eager: mlx.core.if
  edge_wgsl:
    template: unary
    expr: buf_in0_f32[in0_offset]
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
