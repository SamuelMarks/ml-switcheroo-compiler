operation: get_neighbor
description: Helper function that computes the mesh indices of a neighbor.
std_args:
  - idx
  - mesh
  - axis_name
  - direction
type: function
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
