operation: histogram2d
description: The Histogram2d operation.
std_args: []
variants:
  numpy:
    generator: np.histogram2d
  cupy: {}
  dask: {}
  tensorflow:
    eager: tf.histogram2d
  keras:
    eager: keras_ops.histogram2d
  pytorch:
    eager: torch.histogram2d
  jax:
    eager: jax.numpy.histogram2d
  mlx:
    eager: mlx.core.histogram2d
  edge_onnx:
    generator: histogram2d
  edge_stablehlo:

    opcode: stablehlo.histogram2d
  edge_wgsl:
    template: binary
    expr: f32(buf_in0_f32[in0_offset] != 0.0 && buf_in1_f32[in1_offset] != 0.0)
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
