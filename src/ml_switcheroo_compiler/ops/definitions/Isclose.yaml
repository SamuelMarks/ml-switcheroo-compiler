operation: isclose
description: The Isclose operation.
std_args: []
variants:
  cupy:
    eager: cp.isclose
  dask:
    eager: da.isclose
  keras:
    eager: keras_ops.isclose
  numpy:
    eager: numpy.isclose
  tensorflow:
    eager: tf.isclose
  pytorch:
    eager: torch.isclose
  jax:
    eager: jax.numpy.isclose
  mlx:
    eager: mlx.core.isclose
  edge_onnx:
    generator: isclose
  edge_stablehlo:

    opcode: stablehlo.isclose
  edge_wgsl:
    template: binary
    expr: f32(buf_in0_f32[in0_offset] != 0.0 && buf_in1_f32[in1_offset] != 0.0)
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
