operation: result_type
description: Returns the dtype that results from applying type promotion rules (see
  type-promotion) to the arguments.
std_args: []
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
