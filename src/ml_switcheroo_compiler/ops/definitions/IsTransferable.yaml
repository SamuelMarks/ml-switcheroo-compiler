operation: IsTransferable
description: States that `source` layout must be transferable across memory spaces
  to `target` layout.
std_args:
  - self
  - source
  - target
  - shape
type: class
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
