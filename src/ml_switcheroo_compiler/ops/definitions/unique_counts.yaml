operation: unique_counts
description: Returns the unique elements of an input array ``x`` and the corresponding
  counts for each unique element in ``x``.
std_args:
  - is_variadic: false
    kind: positional_or_keyword
    name: x
    type: array
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
