operation: CudnnIsAcceptable
description: Check if tensor is acceptable for CuDNN.
std_args:
- tensor
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
