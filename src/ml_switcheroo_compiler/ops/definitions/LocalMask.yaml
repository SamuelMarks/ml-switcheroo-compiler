operation: LocalMask
description: Lazy local mask, prevents model from attending to tokens outside window.
std_args:
- self
- shape
- window_size
- offset
- shard_count
type: class
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
