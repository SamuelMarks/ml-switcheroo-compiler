operation: Equals
description: States that `lhs` and `rhs` are equal.
std_args:
- self
- lhs
- rhs
type: class
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
