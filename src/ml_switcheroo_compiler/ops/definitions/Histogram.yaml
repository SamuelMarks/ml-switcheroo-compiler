operation: histogram
description: The Histogram operation.
std_args: []
variants:
  numpy:
    generator: np.histogram
  cupy: {}
  dask: {}
  tensorflow:
    eager: tf.histogram
  keras:
    eager: keras_ops.histogram
  pytorch:
    eager: torch.histogram
  jax:
    eager: jax.numpy.histogram
  mlx:
    eager: mlx.core.histogram
  edge_onnx:
    generator: histogram
  edge_stablehlo:

    opcode: stablehlo.histogram
  edge_wgsl:
    template: binary
    expr: f32(buf_in0_f32[in0_offset] != 0.0 && buf_in1_f32[in1_offset] != 0.0)
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
