operation: colocated_python_class
description: Creates a wrapper class that executes the given Python class methods
  on the same devices as the arguments.
std_args:
- cls
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
