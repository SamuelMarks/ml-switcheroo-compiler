operation: Divides
description: States that the `expr` tiling is a divisor of `tiling_multiple`.
std_args:
  - self
  - expr
  - tiling_multiple
type: class
variants:
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
