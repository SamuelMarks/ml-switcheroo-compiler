operation: registers_shape
description: Returns the shape of the register array needed to represent an array
  of the given logical shape.
std_args:
- self
- shape
type: function
variants:
  edge_wasm_simd:
    template: unary
    simd_expr: in0_val
    scalar_expr: in0_val
  llvm_cpp:
    template: unary
    scalar_expr: in0_val
