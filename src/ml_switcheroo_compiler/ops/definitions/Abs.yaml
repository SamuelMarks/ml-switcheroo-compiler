operation: Abs
description: The Abs operation.
std_args:
- x
variants:
  cupy:
    eager: cp.abs
  dask:
    eager: da.abs
  edge_onnx:
    generator: Abs
  edge_stablehlo:
    generator: stablehlo.abs
  keras:
    eager: keras_ops.abs
  tensorflow:
    eager: tf.abs
  pytorch:
    eager: torch.abs
  jax:
    eager: jax.numpy.abs
  numpy:
    eager: numpy.abs
  mlx:
    eager: mlx.core.abs
  edge_wgsl:
    template: unary
    expr: abs(buf_in0_f32[in0_offset])
  edge_wasm_simd:
    template: unary
    simd_expr: wasm_f32x4_abs(in0_val)
    scalar_expr: std::abs(in0_val)
  llvm_cpp:
    template: unary
    scalar_expr: std::abs(in0_val)
autodiff:
  vjp_rule_id: Abs_vjp
  jvp_rule_id: Abs_jvp
  vjp:
  - Multiply($cotangent, Sign($input[0]))
  jvp: Multiply($tangent[0], Sign($input[0]))
signature:
  args: []
  input_tensors:
  - x
  output_tensors:
  - out
  has_variadic_args: false
  has_variadic_kwargs: false
  keyword_constraints: {}
  keyword_defaults: {}
dtype_rules:
  promotion_matrix: standard
  allowed_input_dtypes:
  - all
  output_dtype_inference: in0
shape_signature:
  pattern: in0
  symbolic_expression: '[*shape] -> [*shape]'
  category: elementwise
invariants:
  is_commutative: false
  is_associative: false
  is_idempotent: true
  identity_value: null
  absorbing_value: null
